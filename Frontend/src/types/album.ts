export interface Album {
  albumID: number;
  albumName: string;
  title: string;
  albumItemImage?: string;
  releaseDate: string;
  uploadAt: string;
  artistID: number;
  artistName: string;
  userID: string;
}